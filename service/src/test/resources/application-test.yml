spring:
  datasource:
    url: jdbc:h2:mem:testdb?currentSchema=public&user=sa&password=password
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: false
